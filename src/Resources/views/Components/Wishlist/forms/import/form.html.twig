<div class="card card-md">
    <div class="card-body">
        {{ form_start(form, { attr: { class: 'import-wishlist ui form', id: "wishlist_import_form" } }) }}
            <div class="mb-3 field text-center">
                {{ form_widget(form.wishlist_file, { attr: { class: "btn btn-outline-gray col-6 col-xl-6,",  } } ) }}
            </div>
            <div class="mb-3 field text-center">
                {{ form_widget(form.wishlists, { attr: { class: "btn btn-outline-gray col-6 col-xl-6",  } } ) }}
            </div>
            <div class="mb-3 field text-center">
                {{ form_widget(form.submit, { attr: { class: "btn btn-primary",  } } ) }}
            </div>
        {{ form_end(form) }}
    </div>
</div>
